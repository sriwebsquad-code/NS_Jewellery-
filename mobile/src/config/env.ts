// Environment Configuration

export const ENV = {
  // Use local IP for local development or the Render URL for production.
  // We avoid process.env in Expo bare/managed without specific dot-env plugins to keep it reliable.
  API_URL: 'https://ns-jewellery.onrender.com/api',
  BASE_URL: 'https://ns-jewellery.onrender.com',
};
